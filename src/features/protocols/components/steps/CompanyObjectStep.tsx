import { useFormContext } from 'react-hook-form';
import type { Company, CompanyObject } from '../../../../types/companies';
import type { ProtocolWizardForm } from '../wizardTypes';

const input = 'w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm focus:border-eco-500 focus:outline-none focus:ring-4 focus:ring-eco-100';
type Props = { companies: Company[]; objects: CompanyObject[]; loading: boolean; onCompanyChange: (id: string) => void };
const CompanyObjectStep = ({ companies, objects, loading, onCompanyChange }: Props) => { const { register, watch } = useFormContext<ProtocolWizardForm>(); const companyId = watch('companyId'); const objectId = watch('objectId'); const company = companies.find((item) => item.id === companyId); const object = objects.find((item) => item.id === objectId); return <section><h3 id="wizard-step-title" tabIndex={-1} className="text-xl font-black">Компания и объект</h3><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="text-sm font-bold">Компания *<select {...register('companyId')} onChange={(event) => onCompanyChange(event.target.value)} className={`${input} mt-1.5`}><option value="">Выберите компанию</option>{companies.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="text-sm font-bold">Объект компании *<select {...register('objectId')} disabled={!companyId || loading || !objects.length} className={`${input} mt-1.5`}><option value="">{loading ? 'Загрузка…' : 'Выберите объект'}</option>{objects.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label>{companyId && !loading && !objects.length && <div role="alert" className="md:col-span-2 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm font-semibold text-amber-900">У выбранной компании нет зарегистрированных объектов. Создайте объект в разделе «Компании».</div>}<label className="text-sm font-bold">Адрес объекта<input readOnly value={object?.address || ''} className={`${input} mt-1.5 bg-slate-50`} /></label><label className="text-sm font-bold">БИН<input readOnly value={company?.bin || ''} className={`${input} mt-1.5 bg-slate-50`} /></label><label className="text-sm font-bold">Заказчик<input {...register('customer')} className={`${input} mt-1.5`} /></label><label className="text-sm font-bold">Основание проведения испытаний<input {...register('basis')} className={`${input} mt-1.5`} /></label></div></section>; };
export default CompanyObjectStep;
