import { useFormContext } from 'react-hook-form';
import type { LaboratoryEmployee, LaboratoryListItem } from '../../../../types/laboratories';
import type { ProtocolWizardForm } from '../wizardTypes';
const input = 'w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm focus:border-eco-500 focus:outline-none focus:ring-4 focus:ring-eco-100';
type Props = { laboratories: LaboratoryListItem[]; employees: LaboratoryEmployee[]; loading: boolean; onLaboratoryChange: (id: string) => void };
const LaboratoryExecutorStep = ({ laboratories, employees, loading, onLaboratoryChange }: Props) => { const { register, watch } = useFormContext<ProtocolWizardForm>(); const laboratoryId = watch('laboratoryId'); const laboratory = laboratories.find((item) => String(item.id) === laboratoryId); return <section><h3 id="wizard-step-title" tabIndex={-1} className="text-xl font-black">Лаборатория и исполнитель</h3><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="text-sm font-bold">Лаборатория *<select {...register('laboratoryId')} onChange={(event) => onLaboratoryChange(event.target.value)} className={`${input} mt-1.5`}><option value="">Выберите лабораторию</option>{laboratories.filter((item) => item.active).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="text-sm font-bold">Исполнитель *<select {...register('executorId')} disabled={!laboratoryId || loading} className={`${input} mt-1.5`}><option value="">{loading ? 'Загрузка…' : 'Выберите сотрудника'}</option>{employees.filter((item) => item.active).map((item) => <option key={item.id} value={item.id}>{item.fullName} · {item.position}</option>)}</select></label><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-bold uppercase text-slate-400">Руководитель лаборатории</p><p className="mt-1 font-semibold">{laboratory?.laboratoryHeadName || '—'}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-bold uppercase text-slate-400">Аккредитация</p><p className="mt-1 font-semibold">{laboratory?.accreditationNumber || '—'} · до {laboratory?.accreditationValidUntil || '—'}</p></div></div></section>; };
export default LaboratoryExecutorStep;
