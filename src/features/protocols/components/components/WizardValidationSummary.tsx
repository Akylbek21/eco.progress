export type WizardIssue = { message: string; step: number };
type Props = { issues: WizardIssue[]; onGoTo: (step: number) => void };
const WizardValidationSummary = ({ issues, onGoTo }: Props) => issues.length ? <div role="alert" className="rounded-2xl border border-rose-200 bg-rose-50 p-4"><h4 className="font-black text-rose-900">Что необходимо исправить</h4><ul className="mt-3 space-y-2">{issues.map((issue,index) => <li key={`${issue.step}-${issue.message}-${index}`} className="flex items-center justify-between gap-3 rounded-xl bg-white px-3 py-2 text-sm text-rose-900"><span>{issue.message}</span><button type="button" onClick={() => onGoTo(issue.step)} className="shrink-0 font-bold text-eco-700">Перейти к исправлению</button></li>)}</ul></div> : <div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-4 font-bold text-emerald-900">Все обязательные данные заполнены. Можно переходить к созданию.</div>;
export default WizardValidationSummary;
