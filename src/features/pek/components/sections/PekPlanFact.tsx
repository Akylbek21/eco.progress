import { Link } from 'react-router-dom';
import type { PekPlanFactRow } from '../../api/pekContracts';

const status:Record<string,string>={NOT_STARTED:'Не начато',PARTIAL:'Частично',COMPLETED:'Выполнено',OVERFULFILLED:'Перевыполнено'};
const PekPlanFact=({rows}:{rows:PekPlanFactRow[]})=><div className="overflow-x-auto"><table className="min-w-[1050px] w-full text-sm"><thead><tr>{['Позиция','Точка/источник','Периодичность','План событий','Факт событий','Выполнение событий','План показателей','Найдено','Полнота показателей','Статус','Протоколы','Проблемы'].map(x=><th key={x} className="px-3 py-2 text-left">{x}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={row.id} data-row-key={row.id} className="border-t"><td className="px-3 py-3">{row.controlItem}</td><td className="px-3 py-3">{row.source}</td><td className="px-3 py-3">{row.frequency}</td><td className="px-3 py-3">{row.plannedEvents}</td><td className="px-3 py-3">{row.actualEvents}</td><td className="px-3 py-3 font-bold">{row.eventCompletionPercent}%</td><td className="px-3 py-3">{row.plannedIndicators}</td><td className="px-3 py-3">{row.foundIndicators}</td><td className="px-3 py-3 font-bold">{row.indicatorCompletenessPercent}%</td><td className="px-3 py-3">{status[row.status]}</td><td className="px-3 py-3">{row.protocolIds.map(id=><Link key={id} to={`/staff/protocols/${id}`} className="mr-2 font-bold text-eco-700">№{id}</Link>)}</td><td className="px-3 py-3">{row.issueCount}</td></tr>)}</tbody></table>{!rows.length&&<p className="p-5 text-center text-slate-500">События контроля ещё не собраны</p>}</div>;
export default PekPlanFact;
