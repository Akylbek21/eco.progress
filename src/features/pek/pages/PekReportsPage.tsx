import { keepPreviousData, useQuery } from '@tanstack/react-query';
import { Link, useSearchParams } from 'react-router-dom';
import { pekKeys } from '../api/pekQueryKeys';
import { pekService } from '../api/pekService';
import type { PekPeriodType, PekReportFilters, PekReportStatus } from '../api/pekContracts';
import { PekLoading, PekPageHeader, PekReadiness, PekState, PekStatusBadge } from '../components/common/PekUi';

const PekReportsPage = () => {
  const [params, setParams] = useSearchParams();
  const filters: PekReportFilters = { search: params.get('search') || undefined, companyId: Number(params.get('companyId')) || undefined, objectId: Number(params.get('objectId')) || undefined, periodType: (params.get('periodType') || undefined) as PekPeriodType | undefined, year: Number(params.get('year')) || undefined, quarter: Number(params.get('quarter')) || undefined, status: (params.get('status') || undefined) as PekReportStatus | undefined, onlyWithErrors: params.get('onlyWithErrors') === 'true' || undefined, onlyOverdue: params.get('onlyOverdue') === 'true' || undefined, page: Number(params.get('page')) || 0, size: Number(params.get('size')) || 20, sort: params.get('sort') || 'updatedAt,desc' };
  const query = useQuery({ queryKey: pekKeys.reports(filters), queryFn: ({ signal }) => pekService.getReports(filters, signal), placeholderData: keepPreviousData });
  const update = (key: string, value: string) => { const next = new URLSearchParams(params); value ? next.set(key, value) : next.delete(key); if (key !== 'page') next.set('page','0'); setParams(next,{replace:true}); };
  return <div className="space-y-5"><PekPageHeader title="Отчёты ПЭК" description="Сбор, проверка и отправка отчётов" actions={<Link to="/staff/pek/reports/new" className="rounded-full bg-eco-600 px-5 py-2.5 text-sm font-bold text-white">Создать отчёт ПЭК</Link>}/><section className="grid gap-3 rounded-2xl border bg-white p-4 md:grid-cols-4 xl:grid-cols-7"><input aria-label="Поиск" placeholder="Поиск" value={filters.search || ''} onChange={(event) => update('search',event.target.value)} className="rounded-xl border px-3 py-2"/><input aria-label="Компания" placeholder="Компания" value={params.get('companyId') || ''} onChange={(event) => update('companyId',event.target.value)} className="rounded-xl border px-3 py-2"/><input aria-label="Объект" placeholder="Объект" disabled={!filters.companyId} value={params.get('objectId') || ''} onChange={(event) => update('objectId',event.target.value)} className="rounded-xl border px-3 py-2"/><select aria-label="Период" value={filters.periodType || ''} onChange={(event) => update('periodType',event.target.value)} className="rounded-xl border px-3 py-2"><option value="">Период</option><option value="QUARTER">Квартал</option><option value="YEAR">Год</option></select><input aria-label="Год" type="number" value={filters.year || ''} onChange={(event) => update('year',event.target.value)} className="rounded-xl border px-3 py-2"/><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={Boolean(filters.onlyWithErrors)} onChange={(event) => update('onlyWithErrors',event.target.checked ? 'true' : '')}/>С ошибками</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={Boolean(filters.onlyOverdue)} onChange={(event) => update('onlyOverdue',event.target.checked ? 'true' : '')}/>Просроченные</label></section>{query.isLoading ? <PekLoading/> : query.isError ? <PekState title="Не удалось загрузить отчёты" retry={() => void query.refetch()}/> : !query.data?.content.length ? <PekState title="Отчёты ПЭК пока не создавались"/> : <div className="overflow-x-auto rounded-2xl border bg-white"><table className="min-w-[1200px] w-full text-sm"><thead className="bg-slate-50 text-left"><tr>{['Номер/период','Компания','Объект','Программа','Revision','Статус','Готовность','Ошибки','Предупреждения','Превышения','Ответственный','Срок','Следующее действие'].map((x)=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody>{query.data.content.map((item)=><tr key={item.id} className="border-t"><td className="px-4 py-3 font-bold">{item.number}<br/><span className="font-normal text-slate-500">{item.periodStart} — {item.periodEnd}</span></td><td className="px-4 py-3">{item.company.name}</td><td className="px-4 py-3">{item.object.name}</td><td className="px-4 py-3">{item.program?.name || '—'}</td><td className="px-4 py-3">{item.revision}</td><td className="px-4 py-3"><PekStatusBadge status={item.status}/></td><td className="px-4 py-3"><PekReadiness value={item.readinessPercent} valid={item.valid}/></td><td className="px-4 py-3 font-bold text-rose-700">{item.blockingIssueCount}</td><td className="px-4 py-3">{item.warningCount}</td><td className="px-4 py-3">{item.exceedanceCount}</td><td className="px-4 py-3">{item.responsible?.name || '—'}</td><td className="px-4 py-3">{item.dueDate || '—'}</td><td className="px-4 py-3"><Link className="font-bold text-eco-700" to={`/staff/pek/reports/${item.id}`}>{item.availableActions?.[0]?.label || 'Открыть'}</Link></td></tr>)}</tbody></table></div>}</div>;
};
export default PekReportsPage;
