package kz.ecoprogress.documentflow.document;

/** Which {@link DocumentDirection}s a given {@link DocumentType} may legally be created with. */
public enum AllowedDirection {
    IN,
    OUT,
    BOTH
}
